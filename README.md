# java-lab-6
Description Sort Array Program Description Given an array A of N integers, your task is to sort the array in ascending order. Input Format The first line contains an integer, denoting the size of the array N. The second line contains N space-separated integers representing the array's elements Output Format
